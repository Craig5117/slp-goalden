<div class="wrapper" style="border: 3px #516360 solid;">
    <div class="student-name-wrapper">
        <h2 class="student-name" style="border: 3px #516360 solid;"> {{studentName}} </h2>
    </div>

    <a class="waves-effect waves-light btn modal-trigger" href="/students/add-studentgoal/{{studentId}}">Add Goal to Student</a>
    {{!-- {{#if studentGoals }} --}}
    <div class="goals-wrapper">
        {{#each studentGoals as |studentGoal|}}
        <div class="goals-input">
            <h4> {{studentGoal.goal.name}} </h4>
            <p> {{studentGoal.goal.description}} </p>
        </div>
    

        <a href="/students/trial-submit/{{studentGoal.id}}" class="btn waves-effect waves-light">+ Trial Data</a>

        <div class="student-trials">
            {{#each studentGoal.trials as |trial|}}
            <h4> Date of Trials </h4>
            <p> {{trial.date}} </p>
            <h4> Successful Trials </h4>
            <div>
                <p> {{trial.successful}} </p>
                <p> {{trial.percent}} </p>
            </div>
            {{/each}}
        </div>
        {{/each}}
    </div>
</div>




<script src="/javascript/single-student.js"></script>